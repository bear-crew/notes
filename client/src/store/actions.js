import { ACTION_CHANGE_NOTE_ID, ACTION_CHANGE_IS_OPEN } from '../index.js'

const actionChangeNoteId = {
    type: ACTION_CHANGE_NOTE_ID,
    payload: null
};

const actionChangeIsOpen = {
    type: ACTION_CHANGE_IS_OPEN,
    payload: null
}